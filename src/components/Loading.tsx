import { LoadingIcon } from '../utils/icons';

const Loading = () => {
  return (
    <div className="w-full h-full flex justify-center items-center">
      <LoadingIcon className="text-5xl text-gray-500" />
    </div>
  );
};

export default Loading;
